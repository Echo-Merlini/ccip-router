[
  { "file": "src/db/schema.ts",
    "find": "PRIMARY KEY (input_hash, namespace, value)",
    "replace": "PRIMARY KEY (input_hash, namespace)",
    "class": "guard", "label": "revert PK widen",
    "must": "a differing value is a divergence and both MUST be retained (not collapsed)" },
  { "file": "src/db/sqlite.ts",
    "find": "records.length === 1",
    "replace": "records.length >= 1",
    "class": "guard", "label": "getRecordState boundary",
    "must": "record-store state MUST be expressible as single | divergent | absent" },
  { "file": "src/db/sqlite.ts",
    "find": "INSERT OR IGNORE INTO records\n          (input_hash",
    "replace": "INSERT OR REPLACE INTO records\n          (input_hash",
    "class": "guard", "label": "keep-first -> keep-last",
    "must": "observation-identical collapse keeps the first valid record" },
  { "file": "src/db/sqlite.ts",
    "find": "AND signature  != @signature",
    "replace": "AND signature  = @signature",
    "class": "guard", "label": "double-sign detector inverted",
    "must": "same-signer equivocation is flagged; honest different-signer divergence is not" },
  { "file": "src/db/sqlite.ts",
    "find": "WHERE namespace = @namespace AND timestamp > @since",
    "replace": "WHERE namespace = @namespace AND timestamp >= @since",
    "class": "guard", "label": "sync cursor off-by-one",
    "must": "replication is idempotent/convergent — sync returns records strictly after the cursor" }
]
